pre-commit:
  parallel: false
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check --quiet
    clippy:
      glob: "*.rs"
      run: cargo clippy --quiet -- -D warnings
    test:
      glob: "*.rs"
      run: cargo test --quiet
